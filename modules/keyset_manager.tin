#ALIAS {^set{kill|load|reload} %S$}
{
	#nop {reload all modules};
	#if {"%0" == "setreload all"}
	{
		#script {for keyset in ../keysets/*.tin ; do echo "#class {$(basename "$module" .tin)} {kill};#class {$(basename "$module" .tin)} {read} {$module}";done};
		#echo {All modules reloaded}
	};
	#else
	{
		#nop {Kill, load, or reload a specified module.};
		#if {"%1" == "{kill|reload}"}
		{
			#class {%2} {kill};
			#showme {Class %2 has been killed.}
		};
		#if {"%1" == "{load|reload}"}
		{
			#showme {Loading keysets/%2.tin};
			#class {%2} {read} {keysets/%2.tin};
			#class {gameData} {open}
		}
	}
}
{9}

